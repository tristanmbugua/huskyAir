package com.TM.spring.jpa;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class HuskyAIR_Driver {

	public static void main(String[] args) {
		SpringApplication.run(HuskyAIR_Driver.class, args);
	}

}

//Store flights classes locally.
//Have the first and last name saved locally.
//On Registration, have a user define first and last name.
//Create redirect pages
//Make a foreign key using thymeleaf